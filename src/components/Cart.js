import React from "react";

export default function Cart(...cart) {
  // const listPrestations = cart.prestations.map((prestation) => {
  //   return <p key={prestation.id}>{prestation.name}</p>;
  // });

  return (
    <div>
      {/* {listPrestations} */}

      <div>Total :</div>
    </div>
  );
}
